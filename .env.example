# 复制为 .env.local 后填写（不要提交 .env.local）
# 修改后需重启：npm run dev（Next 不会热重载新环境变量）
#
# Key 优先级（服务端）：请求头 > SILICONFLOW_API_KEY > 代码默认
# 客户端进页自动带请求头：localStorage 覆盖 > NEXT_PUBLIC_SILICONFLOW_API_KEY > 代码默认
# NEXT_PUBLIC_* 会暴露给浏览器，仅自用站点使用

SILICONFLOW_API_KEY=
# NEXT_PUBLIC_SILICONFLOW_API_KEY=
# 须在硅基流动控制台与文档中核对；旧版 Qwen2.5-72B 等可能已下线导致 404
SILICONFLOW_MODEL=Qwen/Qwen3-14B
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
